{% extends 'base.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('view') }}
{% endblock %}

{% block title %}Project: {{ project.name }}{% endblock %}

{% block body %}
    <h1>Project</h1>

    <div
            id="viewer"
            style="width: 100%;height: 80vh;"
            data-panorama="{{ path('app_media_download', {id: mediaList.0.id}) }}"
    >

    </div>

{% endblock %}
